/* global shoppingList */

// eslint-disable-next-line no-unused-vars
/*eslint-env jquery*/
'use strict'; 
$(document).ready(function() {
  shoppingList.bindEventListeners();
  shoppingList.render();
});
